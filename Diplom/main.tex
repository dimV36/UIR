\documentclass{article}
\usepackage[utf8]{inputenc}

\usepackage[T2A]{fontenc}
\usepackage[russian,english]{babel}

\usepackage{tikz}
\usetikzlibrary{shapes, arrows}
\usetikzlibrary{positioning}

\begin{document}

\tikzstyle{decision} =
        [
                diamond,
                draw,
                fill = green!20,
                text width = 6em,
                text badly centered,
                node distance = 2cm,
                inner sep = 0pt
        ]
\tikzstyle{block} =
        [
                rectangle,
                draw,
                fill = blue!20,
                text width = 10em,
                text centered,
                rounded corners,
                minimum height = 2em
        ]
\tikzstyle{line} =
        [
                draw,
                -latex'
        ]
\tikzstyle{cloud} =
        [
                draw,
                ellipse,
                fill = red!20,
                node distance = 3cm,
                minimum height = 2em
        ]

\begin{tikzpicture}[node distance = 1cm, auto]
        \node [cloud] (start) {Начало};
        \node [block, below of = start] (phase1) {Клиент входит в ОС};
		\node [decision, below of = phase1] (dsacondition) {ЭЦП создана?};
        \node [block, below of = dsacondition, node distance = 2.5cm] (phase2) {Создаётся приватный ключ клиента};
        \node [block, left of = phase2, node distance = 7.5cm] (createdsa) {Создается ЭЦП клиента};
        \node [block, below of = phase2, node distance = 1.5cm] (phase3) {Создаётся запрос на подпись сертификата};
        \node [block, below of = phase3, node distance = 1.5cm] (phase4) {УЦ проверяет ЭЦП клиента};
        \node [block, below of = phase4, node distance = 1.5cm] (phase5) {Выпуск сертификата клиента УЦ};
        %\node [block, left of = phase4, node distance = 5cm] (correction) {};
        \node [decision, below of = phase5, node distance = 3cm] (condition) {Требуется перейти на новый уровень?};
        \node [cloud, below of = condition] (finish) {Конец};
        \path [line] (start) -- (phase1);
		\path [line] (phase1) -- (dsacondition);
		\path [line] (dsacondition) -- node {Да}(phase2);
		\path [line] (dsacondition) -| node [near start] {Нет} (createdsa);
		\path [line] (createdsa) -- (phase2);
        \path [line] (phase2) -- (phase3);
        \path [line] (phase3) -- (phase4);
        \path [line] (phase4) -- (phase5);
        \path [line] (phase5) -- (condition);
        \path [line] (condition) -- node [near start] {Да} (phase2);
        %\path [line] (correction) |- (phase1);
        \path [line] (condition) -- node {Нет}(finish);
\end{tikzpicture}

\end{document}
